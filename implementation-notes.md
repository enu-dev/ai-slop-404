# implementation-notes ｜ 404 Slop Test

> 開発中スクラッチファイル。実装中に「仕様書になかった判断・変更・妥協・意思決定」を逐次記録する。
> handoff.md 完成時に PM 神田が「詰まったこと」「制作日誌」へ畳み込み、本ファイルの実体は成果物に残さない（CLAUDE.md 開発品質ルール⑥）。

---

## なし版 404（plain-404）｜ FE 今村

要件：「普段の組織が本気で作る、手を抜かない汎用404」。Hallmark を使わずに実装。

### 仕様外の判断
- **アイコンに 🔍 絵文字を採用**：要件にアイコン指定なし。汎用404の定番として絵文字を選択。SVG を書き起こすより早く "それっぽく" なるため。← この「手早く埋める判断」自体が、後であり版との比較対象になる。
- **配色 `#667eea → #764ba2` グラデーション**：要件に配色指定なし。意図的に「最頻出の AI 配色」を選択。なし版が極端に良くても悪くてもフェアでないため、"標準的に出てくる見た目" の代表性を優先（FE 小山の baseline 注文への対応）。
- **背景・見出し数字・ボタンすべてに同一グラデを適用**：迷ったが「統一感が出る」という default 判断で全面採用。
- **アイコンに float アニメーション付与**：要件になし。「静止画面は間が持たない」という default 判断で動きを追加。

### 妥協点
- ボタン遷移先は `href="#"`：本作品は比較ビューア内で表示するため実遷移先を持たない。実用404としては未完だが、比較用途では許容と判断。

### baseline として残す観点
- なし版は「ダサく作る」のではなく「競合製品レベルに整った、しかし没個性な AI default」を狙った。
- 判断の一つ一つが "無難な既定値" に倒れている点を、あり版との差分で可視化するのが本作品の主旨。

---

## あり版 404（hallmark-404）｜ FE 今村

要件：「意図のあるデザインで作る」。Hallmark スキルを実際に適用して実装。

### 仕様外の判断（Hallmark が導いた選択）
- **ジャンル＝editorial / テーマ＝Newsprint を採用**：要件に配色・書体の指定なし。Hallmark の signal 判定で editorial（既定）、テーマはなし版が紫グラデ・幾何サンセリフのため、対比として暖色のサーモンペーパー＋セリフ書体（Playfair Display + Crimson Pro）を選択。
- **最大の意思決定 ＝「404」の数字を主役から降ろした**：なし版（および世の中の AI 製404）は巨大グラデ数字を主役にする。あり版は数字を `--color-paper-3` の淡いウォーターマークに落とし、画面外へ bleed させ、**人間向けメッセージを主役**に据えた。要件には書かれていない設計判断だが、「意図のあるデザイン」を最も体現する選択としてここを軸にした。
- **中央寄せを捨てて左寄せ非対称に**：なし版は完全中央寄せ（AI default）。あり版は editorial の鉄則どおり左バイアス＋非対称。
- **ボタンをピル型グロー → 角ありソリッド＋テキストリンクの2段構えに**：プライマリは角ゼロのソリッドブロック（editorial テーマは角丸を持たない）、セカンダリは hairline 下線つきの本物のテキストリンク。glow（box-shadow ハロ）は不使用。
- **エントランスは1回のみ**：editorial の「静かなオーケストレーション」に従い、kicker→body の rise を1回だけ。`prefers-reduced-motion` で完全に無効化。
- **コピーを定型文から書き起こした**：「ページが見つかりませんでした」→「そのページは、ここにはありません。」宣言的で少し編集的な語り口に。

### CTO 黒木との確認事項（開発品質ルール②）
- **`oklch()` カラー記法を使用**：Hallmark のトークン体系が OKLCH 必須。2023年以降の仕様のため MDN を確認 → Chrome 111+ / Safari 15.4+ / Firefox 113+ で対応済み、現行ブラウザはカバー。実機確認項目として QA 浜田へ申し送り。
- `clamp()`・flexbox は枯れた仕様、追加確認不要。

### 妥協点
- ボタン遷移先は `href="#"`：なし版と同じく比較ビューア用のため実遷移先なし。
- Google Fonts（Playfair Display / Crimson Pro / Inter）を CDN 読み込み：オフライン時は OS のセリフ／サンセリフにフォールバック。日本語グリフは元々 OS の Mincho にフォールバックする設計（意図どおり）。

---

## 比較ビューア（index）｜ FE 小山

要件：なし版/あり版を1ページ内で切り替える比較ビューア。差別化要素そのもの。

### 仕様外の判断
- **切替方式＝トグル（セグメンテッドコントロール）で全画面スワップ**：UIUX 宮崎の指示どおり。横並び同時表示は採らず、トグルで質感の変化を一点見せにする。
- **2ページを iframe で分離読込**：藤井（アーキ）の構成判断どおり。各404が `tokens.css` 等で CSS を干渉させないための分離。
- **ビューアのバーを暗色ニュートラルにした**：要件に指定なし。どちらの404（紫グラデ/サーモン）とも競合せず、「ギャラリーの額縁」として中立に見せるための判断。
- **トグルを `<button>` + `aria-pressed` で実装**：role="group" でグルーピング。キーボード操作はネイティブの Enter/Space で成立。

### 妥協点
- iframe は file:// でも GitHub Pages でも動作する想定だが、一部ブラウザの file:// 制限（fonts.googleapis.com への fetch 等）は実機確認が必要 → QA 浜田へ申し送り。
- ビューアは Hallmark 非適用（額縁のため意図的にプレーン）。ただし `transition` はプロパティ明示・glow なし・絵文字なしで slop を持ち込んでいない。

---

## 比較ビューア（index）｜ FE 小山

（実装後に追記）
